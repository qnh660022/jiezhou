# 芥舟 · V2.8.1 版提示词工程（施工规格书 · 地基与账本域）

> **版本号**：V2.8.1（r1）
> **应用基线**：`pubspec.yaml version` 交付时更新为 `2.8.1+2810`（沿用 `x*1000+y*100+z*10+w` 惯例）。
> **代码基线**：master 分支（2026-09-19）。前置基线：V2.7.2 施工中（S5 已完工，其余以《V2.7.2验收报告.md》进度为准）；V2.8.1 与 V2.7.2 **并行施工**，冲突规避见 §1.4 并行协议（本版核心创新条款）。
> **本版主线**：P0 止血 + 三大基建（玻璃质感 / 弹层四层规范 / 图标体系）+ 账本域页面重设计（记一笔键盘化 · 明细页 · 首页仪表盘 · 统计下钻 · 账单抽屉 · 设置页）+ 微交互包 A
> **文档性质**：施工规格书。逐阶段给出：目标与定位 / 前置依赖 / 涉及文件清单 / 数据契约（含 DDL）/ 算法规格（含 Dart 签名）/ UI 规格 / 同步登记 / 测试用例清单 / 验收标准 / 禁止事项。施工不得跨阶段跳跃。
> **视觉依据**：`docs/V2.8/preview/index.html`（场景 A–E）+ `docs/V2.8/preview/pages2.html`（场景 F–N），每场景头部标注所属阶段。
> **设计输入**：《UI-UX整改方案-v2.7.1.md》（v2）、《UI-UX重设计-第二批页面.md》、三轮用户裁决记录（见附录 T7）。
> **编制时间**：2026-09-19

---

## 〇、版权与合规红线（效力高于本文件其余一切条款）

1. 沿用《V2.7.2版提示词工程》§〇 全部条款（零复制代码 / 零商标 / 文案原创 / 发现即停），此处不再重复；
2. **本版专项**：iconfont 生成工具（iconfont.cn / svgtofont 等）仅为构建期工具链，**不得进入 pubspec 依赖树**；图标 SVG 源必须为本仓库原创绘制或使用本仓库作者拥有权利的素材；
3. 本版**零新增第三方依赖**（用户裁决：维持纯 PIN，白名单机制本期空表运行，见 §2.4）。

---

## 一、范围裁定与并行协议

### 1.1 纳入范围（IN，12 项）

| 编号 | 内容 | 阶段 | 优先级 |
|---|---|---|---|
| A1 | P0 止血包（4 项单点缺陷） | S1 | P0 |
| A2 | 玻璃质感基建：GlassTokens + GlassSurface + 三组件替换 + 页面转场 | S2 | P0 |
| A3 | 弹层四层规范：showConfirmSheet / SnackBar 组件 / 权限引导 + 账本·设置域 AlertDialog 迁移 | S3 | P0 |
| A4 | 图标体系基建：JieZhouIcons.ttf + AppIcons 常量表 + 账本域首批替换 | S4 | P0 |
| A5 | 记一笔页键盘化（**完全替换**旧 11 区块表单，无双页） | S5 | P0 |
| A6 | 明细页：SwipeableBillTile + 筛选收敛 + 双级吸顶 + 时间筛选 + 批量（删除/改分类） | S6 | P0 |
| A7 | 账本首页三层仪表盘 + 功能入口收敛（工具箱/团管理去重/QR 统一/删团滑动化） | S7 | P1 |
| A8 | 统计页图表下钻 + 洞察语（仅环比）+ 分类子预算（用户自选，上限 5） | S8 | P1 |
| A9 | 账单详情抽屉重设计（含结清前置/删除确认/精要插槽） | S9 | P1 |
| A10 | 设置页状态摘要卡 + 组内搜索 + 工具箱入口 | S10 | P1 |
| A11 | 微交互包 A：PressableScale / EmptyState 浮动玻璃盘 / SnackBar 玻璃化 | S11 | P2 |
| A12 | 同步登记：SubBudgets 新表全量登记（schema N→N+1） | S8 内 | P0 |

### 1.2 排除范围（OUT，施工中不得实现）

| 编号 | 排除项 | 依据 |
|---|---|---|
| O1 | 旅伴空间详情任何改动（含徽章图标化） | 用户裁决：M 仅登记，V2.8.3 候选 |
| O2 | 生物识别（local_auth）| 用户裁决：维持纯 PIN |
| O3 | 批量导出选中账单 | 用户裁决：批量仅删除+改分类 |
| O4 | 洞察语「集中日」维度、AI 洞察 | 用户裁决：仅环比；AI 触 V2.7.2 O11 红线延续 |
| O5 | 「催 TA」分享功能 | 用户裁决：从首页方案中移除 |
| O6 | 记一笔双模式（键盘页+完整表单页并存） | 用户裁决：完全替换 |
| O7 | 一切 Web/桌面端适配（desktop_* 文件禁触） | 用户既定方针 |
| O8 | 行程详情页内部结构改动（页签容器归 V2.8.2，且仅页签层） | 三轮裁决 |
| O9 | 全局 Undo 体系 | V2.7.2 O2 延续，不可逆操作强确认兜底 |
| O10 | 底栏/开屏/行程域图标替换（归 V2.8.2） | 拆分裁决 |
| O11 | 依赖白名单外任何新包 | §2.4 |

### 1.3 概念收口规则

1. **GlassSurface 是唯一玻璃实现**：任何页面不得再手写 `BackdropFilter` + 手调 alpha（现存三处由 S2 收编，此后新增视为违规）；
2. **弹层四层**（L1 轻提示 / L2 确认 / L3 操作抽屉 / L4 表单抽屉）是全 App 模态唯一语言；S3 完成后账本·设置域 `AlertDialog` 必须为 0；
3. **图标单一来源**：功能图标岗位只允许 `AppIcons.*` 或 `Icons.*_rounded` 两族，emoji 仅允许出现在内容岗位（行程封面数据/用户输入/EmptyState 兼容参数）；
4. **键盘即保存**：记账键盘 `完成` 键语义 = 保存，不得出现第二个保存主按钮。

### 1.4 V2.7.2 并行协议（本版必须逐日遵守）

**禁触清单**（V2.7.2 未验收阶段占用文件，2.8.1 一律不得修改；完工判定以《V2.7.2验收报告.md》登记为准）：

| 若 V2.7.2 该阶段未验收 | 2.8.1 禁触文件 |
|---|---|
| S1 数据地基 | `tables.dart`、`database.dart`、`sync_models/codec/merger/engine/db_access`、`sync_transport_fake`、`backup_format`、`wishlist_dao/repo`、`tables.dart` |
| S2 顺延引擎 | `day_shift_engine`、`trips_repo.applyDayOps`、`day_ops_sheet` |
| S3 大纲双向 | `outline_parser`、`outline_panel`、`wishlist_repo`（入池接线） |
| S4 拖拽批量 | `trip_detail_screen.dart`、`trips_repo`（reorder/move/batch） |
| S5 攻略双动作 | `trip_guide_screen.dart`、`guide_ref.dart`（已完工 ✅，文件解禁） |
| S6/S7/S8 | `wishlist_panel/assemble_panel/assemble_engine`、`trip_edit_screen` |
| S9 锦囊 | `kit_panel`、`guide_match` |
| S10 精要随卡 | `guide_essence_section` |
| S12 PDF | `pdf_builder`、`trip_export_screen` |

**共享文件协议**：
- `lib/theme/tokens.dart`：2.8.1 S2 以**纯追加**方式并入 `GlassTokens`（不修改任何既有行）；合并前 `git diff` 确认无 V2.7.2 未提交改动；
- `lib/shared/widgets/*`：2.8.1 以新增为主；修改 `sheet.dart`（SheetContainer/SheetSurface 内部替换）前确认 V2.7.2 无未合流改动；
- `expense_edit_screen.dart` / `expenses_screen.dart` / `ledger_home_screen.dart` / `stats_screen.dart` / `budget_screen.dart` / `bill_detail_sheet.dart` / `profile_screen.dart` / `group_list_screen.dart` / `categories_screen.dart`：V2.7.2 全程不占用的账本域文件，2.8.1 主战场；
- **提交纪律**：每阶段分支上先 `flutter analyze` + 对照本清单 `git status`，发现禁触文件被改动立即停止并登记偏差。

---

## 二、基线校准与全局工程约定

### 2.1 基线校准（S1 第一步）

1. 干净工作区 `flutter analyze`（0 error）与全量 `flutter test`，实测用例总数记 `N1`，warning 数记告警基线，写入《V2.8.1验收报告.md》；
2. `grep -rn "AlertDialog(" lib/features/ledger lib/features/settings | wc -l` 结果记为迁移基线 M1（预期 ≈15，S3 完成后必须为 0）；
3. `grep -rn "BackdropFilter(" lib | wc -l` 记玻璃基线 B1（预期 4：glass_app_bar/nav_bar/sheet×2，S2 后业务手写必须为 0）。

### 2.2 不可动摇的架构基线（沿用 V2.7.2 §2.2 全部 + 补充）

| 约定 | 内容 |
|---|---|
| 金额口径 | 一律 `int` 分；仅展示层 `MoneyText/MoneyFormat` 格式化；键盘连算在 int 分域完成 |
| 日期口径 | 行程域 `epochDay`；账单日期沿用既有 `DateTime` 口径不改 |
| 算法层纯净 | `lib/domain/`（含 S8 洞察引擎）纯 Dart 无 IO 无时钟直读 |
| 写入即同步 | 写操作事务提交后 `notifyWrite`；批量逐行补发；删除发墓碑 |
| 单一桥接层 | UI 经 repo 访问数据，禁直连 DAO |
| 主题令牌 | 颜色一律 `ColorScheme` 语义色 + tokens；禁硬编码色值（S1 修掉最后一处 ai_cards:847） |

### 2.3 通用禁止事项

1. 禁止跨阶段施工；S1 未验收不得进入 S2；
2. 禁止修改云端 RLS 既有策略语义（SubBudgets 仅新增策略，克隆既有范式）；
3. 禁止在 `portionsJson` 等既有语义重载字段上叠加新语义；
4. 禁止使用未登记的第三方依赖（本版零新增）；
5. 禁止让 `GlassSurface`/`showConfirmSheet`/`AppIcons` 之外的新共享组件绕过本规格书私自立项。

### 2.4 依赖白名单机制

`docs/V2.8/dependency_whitelist.md`（本版创建，**空表**）：任何未来版本新增依赖必须先在此登记（包名/用途/License/替代方案评估），本期一条不加。生物识别（local_auth）已裁决延后，登记为 V2.8.3+ 候选。

---

## 三、S 阶段总览

### 3.1 依赖图

```
S1 P0止血 ─┬→ S2 玻璃基建 ─┬→ S5 记一笔 ─┬→ S9 账单抽屉
            ├→ S3 弹层规范 ─┤             └→ S6 明细页（依赖 S3 确认弹层）
            └→ S4 图标基建 ─┴→ S7 首页与入口收敛
                             ├→ S8 统计与子预算（依赖 S6 的 URL 参数筛选）
                             └→ S10 设置页（依赖 S4/S7）
S11 微交互A（依赖 S2）
S12 门禁（依赖全部）
```

### 3.2 推荐施工顺序（单人串行）

**S1 → S2 → S3 → S4 → S5 → S6 → S7 → S8 → S9 → S10 → S11 → S12**
（S2/S3/S4 互不依赖，可按任意顺序，但都先于页面阶段）

### 3.3 阶段一览与配额

| 阶段 | 名称 | 硬前置 | 新增用例配额 |
|---|---|---|---|
| S1 | P0 止血包 | 无 | ≥4 |
| S2 | 玻璃基建 | S1 | ≥8 |
| S3 | 弹层规范（含迁移） | S1 | ≥10 |
| S4 | 图标基建 | S1 | ≥6 |
| S5 | 记一笔键盘化 | S2/S3 | ≥18 |
| S6 | 明细页 | S3 | ≥14 |
| S7 | 首页与入口收敛 | S2/S3/S4 | ≥12 |
| S8 | 统计与子预算 | S6 | ≥16 |
| S9 | 账单抽屉 | S3/S4 | ≥8 |
| S10 | 设置页 | S4/S7 | ≥8 |
| S11 | 微交互A | S2 | ≥6 |
| S12 | 门禁与交付 | 全部 | 回归 ≥10（合计 ≥120） |

---

## 四、S1 P0 止血包（A1）

### 4.1 目标

四项用户可直感的单点缺陷修复，全部为小改动，先于一切基建落地。**记账页 PopScope 草稿拦截不在本阶段**（并入 S5 一次做完，避免同文件两次大改）；**账单删除确认不在本阶段**（并入 S3 弹层规范）。

### 4.2 涉及文件清单

| 动作 | 文件 | 内容 |
|---|---|---|
| 修改 | `lib/features/settings/screens/profile_screen.dart` | 预算预警 tile 整体可点 toggle（L86 空 onTap 修复） |
| 修改 | `lib/features/ledger/screens/stats_screen.dart` | 时间范围持久化：SharedPreferences 键 `app.stats.range`（`all/month/lastMonth/year/custom` + custom 起止 `app.stats.range.custom`），进入恢复 |
| 修改 | `lib/features/ai/widgets/ai_cards.dart` | L847 `_primary = 0xFF2E7D5B` → `Theme.of(context).colorScheme.primary` |
| 修改 | `lib/features/ledger/screens/ledger_home_screen.dart` | 预警关闭时点铃铛：SnackBar → L3 抽屉空态卡（「预警已关闭」+ 「去开启」按钮跳预算页） |

### 4.3 验收标准

- [ ] 四项逐一走查通过；`flutter analyze` 0 error；
- [ ] stats 进入时恢复上次范围（冷启动断言）；
- [ ] 新增用例 ≥ 4（toggle / 持久化往返 / 硬编码消除 / 空态抽屉）。

### 4.4 禁止

- 禁止顺手重排 tile 分组（S10 的事）；禁止改 SnackBar 样式（S3 的事）。

---

## 五、S2 玻璃质感基建（A2）

### 5.1 目标

一次性解决「毛玻璃实现不完整」：令牌收口 + 统一组件 + 三处存量替换 + 转场补课。**对外 API 不变**（GlassAppBar/FloatingCapsuleNavBar/showDraggableSheet 调用方零改动）。

### 5.2 涉及文件清单

| 动作 | 文件 |
|---|---|
| 修改 | `lib/theme/tokens.dart`（纯追加 `GlassTokens`，不动既有行） |
| 新建 | `lib/shared/widgets/glass_surface.dart` |
| 修改 | `lib/shared/widgets/glass_app_bar.dart`、`floating_capsule_nav_bar.dart`、`sheet.dart`（内部替换） |
| 修改 | `lib/app.dart` 或主题组装处（`pageTransitionsTheme`） |
| 新建 | `test/shared/glass_surface_test.dart` |

### 5.3 令牌契约（逐字段照抄）

```dart
/// V2.8.1 玻璃令牌：全 App 毛玻璃唯一参数源。
class GlassTokens {
  /// 背景模糊强度（标准档）
  static const double sigma = 28;
  /// 玻璃 tint 不透明度（浅/深色分档：深色更低以透出光感）
  static const double tintAlphaLight = 0.52;
  static const double tintAlphaDark  = 0.42;
  /// 顶边内高光强度（受光边）
  static const double highlightAlphaLight = 0.65;
  static const double highlightAlphaDark  = 0.22;
  /// 全周描边亮线
  static const double edgeAlphaLight = 0.28;
  static const double edgeAlphaDark  = 0.07;
  /// 背景饱和度补偿倍率（blur + saturate 组合）
  static const double saturation = 1.35;
}
/// 挡位：决定阴影强度与 tint 叠加
enum GlassLevel { navBar, sheet, floatingCard, overlay }
```

### 5.4 GlassSurface 组件规格

```dart
/// 统一玻璃表面。结构（外→内）：
/// ClipRRect(borderRadius)
///   └ BackdropFilter(ImageFilter.compose(blur(σ,σ), ColorFilter.saturate(sat)))
///       └ Container(decoration)
///           · color: scheme.surfaceContainerLow.withValues(alpha: tint)
///           · gradient 前景高光: LinearGradient(160°, white α[hi→0], 38% 处截止)
///           · boxShadow 双层: 环影(blur40, offset(0,18), α按level) + 关键影(blur10, offset(0,3))
/// [level] 阴影/tint 修正表：
///   navBar       阴影×0.6  tint×1.0
///   sheet        阴影×1.0  tint×1.0
///   floatingCard 阴影×1.2  tint×0.92
///   overlay      阴影×0.8  tint×0.95
/// 降级：MediaQuery.disableAnimationsOf(context)==true 或 [fallbackOpaque]==true
///      → 跳过 BackdropFilter，直接不透明 surfaceContainer（无模糊纯色）。
class GlassSurface extends StatelessWidget {
  const GlassSurface({required this.child, this.level = GlassLevel.floatingCard,
    this.borderRadius, this.fallbackOpaque = false, super.key});
}
```

### 5.5 三处存量替换规格

1. **GlassAppBar**（glass_app_bar.dart:102-113）：`BackdropFilter(σ18)+α0.72` → `GlassSurface(level: navBar)` 包裹原 Container（去内部 BackdropFilter）；新增滚动加深：offset>36 后 tint α 每像素 +0.0022 封顶 +0.08（与大标题收缩同用 `_offset` ValueNotifier，不新增 listener）；
2. **FloatingCapsuleNavBar**（floating_capsule_nav_bar.dart:37-52）：`σ24+α0.88` → `GlassSurface(level: navBar)`；保留手写阴影删除（由 GlassSurface 双层影替代）；触觉与弹性缩放不动；
3. **sheet.dart**：`SheetContainer` α0.98 → `GlassSurface(level: sheet)`（**解决 2026-09-13「字与底面重叠」的正确姿势：σ28 强模糊 + 饱和补偿，非堆不透明度**）；`SheetSurface` → `GlassSurface` 直接包装（修复其完全无模糊问题）；`showDraggableSheet` 的 `barrierColor` 0.4 → 0.32（预览图中的 barrier 模糊为浏览器演示效果，Flutter 端全屏 BackdropFilter 成本过高，不做——偏差登记注明）；
4. **pageTransitionsTheme**：iOS `CupertinoPageTransitionsBuilder`（获得右滑返回）、Android `ZoomPageTransitionsBuilder`；主题切换蒙版、深浅色分支按 tokens 既有策略。

### 5.6 测试用例清单（≥8）

1. GlassSurface 四 level 的 decoration 断言（tint/阴影参数）；
2. disableAnimations 降级路径（无 BackdropFilter）；
3. fallbackOpaque 强制降级；
4. GlassAppBar 大标题收缩回归（滚动 offset 驱动）；
5. SheetContainer 键盘避让回归（既有行为不变）；
6. 深色模式下 tint/highlight 取深色档；
7. pageTransitionsTheme 双平台断言（Theme 检查）；
8. tokens 纯追加回归（既有令牌值逐一不变）。

### 5.7 验收标准与禁止

- [ ] B1 基线中 4 处 BackdropFilter 收敛为 GlassSurface 内部 1 处实现；`grep BackdropFilter lib --include=*.dart` 除 glass_surface.dart 外为 0；
- [ ] 场景 A（preview/index.html）视觉比对通过；
- [ ] 禁止：修改既有 `AppSchemes`/`AppTextStyles`/`Spacing` 等任何既有令牌值；禁止给业务页面直接暴露 sigma/alpha 参数。

---

## 六、S3 弹层四层规范（A3）

### 6.1 目标

确立 L1–L4 全 App 模态唯一语言，账本+设置域 AlertDialog 清零。

### 6.2 涉及文件清单

| 动作 | 文件 |
|---|---|
| 新建 | `lib/shared/widgets/confirm_sheet.dart`（L2：showConfirmSheet/showDangerConfirm 统一入口，内部走 showDraggableSheet） |
| 新建 | `lib/shared/widgets/app_snack_bar.dart`（L1：showAppSnackBar） |
| 新建 | `lib/shared/widgets/permission_primer.dart`（权限两段式引导卡） |
| 修改 | 账本+设置域 15 处 AlertDialog 迁移（清单见 6.4） |
| 修改 | `lib/features/ledger/widgets/bill_detail_sheet.dart`（删除按钮接 L2 危险态——P0 #3 落地） |
| 新建 | `test/shared/confirm_sheet_test.dart` |

### 6.3 组件契约

```dart
/// L2 确认弹层（替代 AlertDialog 的唯一合法形态）。
/// [danger]=true：状态图标圆盘 error 12% 底 + 确认钮红底 + heavyImpact；
/// [quantityLine]：后果行必须含影响数量（强确认口径，与 V2.7.2 O2 一致）。
Future<bool> showConfirmSheet({
  required BuildContext context,
  required String title,
  required String body,          // 含后果与数量
  String confirmLabel = '确认',
  String cancelLabel = '取消',
  bool danger = false,
  IconData? icon,                // 缺省：danger→AppIcons.trash / 中性→AppIcons.check
});

/// L1 轻提示。语义左缘条 3.5px：[tone] success(green)/destructive(red)/info(primary)。
void showAppSnackBar(BuildContext context, String message,
    {SnackTone tone = SnackTone.info, SnackBarAction? action});
```

### 6.4 迁移清单（本阶段必须清零的 AlertDialog，2.8.1 范围）

`ledger_home_screen`（1）、`group_list_screen`（1）、`categories_screen`（1）、`shared_ledger_sections`（5）、`conflict_badge`（2）、`cloud_account`（4）、`sync_center`（1）、`share_center`（1）、`profile`（1）、`app_lock`（1）——合计 ≈18 处（以 M1 实测为准，全清）。**companions 域 3 处不动（O1）**；trips 域归 V2.8.2。

### 6.5 权限引导规格

- 相机（扫码）/通知（预算预警）两段式：功能页内嵌玻璃卡「需要相机权限 · 用于扫描邀请二维码，照片与数据不会上传 · [去开启]」；系统弹窗拒绝后降级为页内提示条（非弹窗轰炸）；
- 零新依赖：通知申请沿用既有 `flutter_local_notifications` 链路，本组件仅做前置说明 UI。

### 6.6 测试用例清单（≥10）

1. showConfirmSheet 中性/危险两态渲染与返回值；
2. 数量行缺失时 debug 断言（assert）；
3. showAppSnackBar 三 tone 左缘色断言；
4. 账单删除走危险确认（P0 #3 回归）；
5. 迁移后 M1=0（grep 断言测试或门禁脚本）；
6. L3/L4 复用 showDraggableSheet 回归；
7. 权限引导两段式状态机（未申请/已拒/已开）；
8. viewer 角色不出现确认弹层入口（隐藏不置灰口径）；
9. 键盘避让（L4 表单场景回归）；
10. 深色模式弹层对比度。

### 6.7 禁止

- 禁止在 confirm_sheet 内部使用 AlertDialog（自我违例）；禁止迁移清单外的文件顺手改弹层（companions/trips 禁触）。

---

## 七、S4 图标体系基建（A4）

### 7.1 目标

JieZhouIcons 字体落地 + 常量表 + 账本域首批替换。**底栏/开屏/行程域替换归 V2.8.2（O10）**。

### 7.2 资产与文件清单

| 动作 | 文件 |
|---|---|
| 新建 | `design/icons/*.svg`（36 枚源文件：底栏5 + 分类8 + 功能15 + 品牌1 + 预留7，path 契约见 7.3） |
| 新建 | `design/icons/codepoints.json`（码点映射，**提交后冻结**，防漂移） |
| 新建 | `assets/fonts/JieZhouIcons.ttf`（iconfont 工具链产物，构建期工具不入依赖树） |
| 修改 | `pubspec.yaml`（fonts 声明，禁加任何 dependencies） |
| 新建 | `lib/theme/app_icons.dart`（`class AppIcons { static const boat=IconData(0xe900,familyName:'JieZhouIcons'); ... }`） |
| 修改 | `lib/shared/widgets/category_icon_box.dart`（emoji → 图标映射） |
| 修改 | 账单行/明细页/详情抽屉/设置页/首页瓦片 emoji 岗位替换 |
| 新建 | `test/theme/app_icons_test.dart` |

### 7.3 图标风格契约（验收逐条）

- 24×24 网格；主笔画 stroke-width 2、round cap/join；单色 `currentColor`（不锁色值）；
- 每图标至多一处 12% 主色填充点缀；
- 品牌舟形 `boat`：帆两片 + 梯形船体 + 双波纹底（preview 场景 N）；
- 内置分类映射：餐饮 `food` / 交通 `car` / 住宿 `bed` / 门票 `ticket` / 购物 `bag` / 饮品 `cup` / 医疗 `med` / 其他 `tag`——**必须覆盖现有全部 7 内置分类 key**；
- 自定义分类：统一 `tag` 图标（不提供用户自选图标，登记后续版本）。

### 7.4 替换规则

- `CategoryIconBox`：`emoji` 参数保留兼容（内容岗位），新增 `iconKey` 参数优先渲染；内置分类走映射表；
- 账单行/抽屉头部 44px 色块：emoji → 图标 + 语义色 12% 底 + 内高光圈（场景 F 头部样式）；
- EmptyState 不动（S11 处理）。

### 7.5 测试与验收

- [ ] codepoints.json 与 TTF 一致性测试（逐枚 IconData 渲染不抛错 + fallback 字形存在）；
- [ ] 内置分类映射完备性（7/7）；
- [ ] `grep -P "[\x{1F300}-\x{1FAFF}]" lib/features/ledger lib/features/settings --include=*.dart` 计数对比 S4 前下降（残留仅限内容岗位白名单）；
- [ ] 新增用例 ≥6。

### 7.6 禁止

- 禁止引入 flutter_svg / lucide / material_design_icons 等任何包；禁止修改 V2.8.2 范围文件（router.dart 底栏数据、splash_screen、trips_home）；禁止删除 emoji 参数（兼容期保留）。

---

## 八、S5 记一笔页键盘化（A5，本版最大改造）

### 8.1 目标与定位

**完全替换**旧 11 区块纵向表单（用户裁决 O6：无双页）：键盘优先布局，金额与分类同屏，高频动线「金额→分类→完成」零滚动完成；低频字段折叠但可寻（chip 摘要常驻）。

### 8.2 涉及文件清单

| 动作 | 文件 |
|---|---|
| 重写 | `lib/features/ledger/screens/expense_edit_screen.dart`（路由 `/expenses/edit` 与 `?id=`/`?date=` 参数契约不变） |
| 新建 | `lib/features/ledger/widgets/amount_keypad.dart`（自定义键盘组件） |
| 新建 | `lib/features/ledger/widgets/edit_more_sheet.dart`（更多选项折叠组内容，L4 表单抽屉复用） |
| 新建 | `lib/domain/money_expression.dart`（连算纯函数） |
| 修改 | `lib/features/ledger/providers.dart`（默认值记忆 + 草稿状态，以实况文件为准） |
| 新建 | `test/domain/money_expression_test.dart` + `test/features/expense_edit_test.dart` |

### 8.3 布局契约（自上而下，对照场景 B）

1. **金额卡**（玻璃 floatingCard）：分类语义色 icon 底座 + 类型/币种/日期三 chip + 大金额（整数 46 级、小数缩半，`FontFeature.tabularFigures`）+ 右侧 ⌫；退款态整卡语义绿；
2. **摘要 chips 行**（1 行横滑，折叠组已选值常驻）：`💳支付方式` `↩退款/预付态` `👥付款人·分摊模式` `🔗关联行程D?` `📅日期` `▾ 更多选项`——点 chip 即展开对应字段编辑；
3. **分类九宫格** 4×2（8 常用 + 管理）：新图标底座（S4），选中 mint 实底 + 光晕；分类顺序沿用现有九宫格数据源；
4. **标题行**：占位符按分类提示（餐饮→「午餐 / 晚餐 / 零食」…映射表）+ 右侧「⏭ 再记一笔」；
5. **键盘**（玻璃 sheet 档，4×4）：`7 8 9 ⌫ / 4 5 6 ＋ / 1 2 3 － / . 0 备注 完成✓`；
6. 旧 11 区块中的完整编辑控件（币种 chips+汇率抽屉、退款预付互斥卡、支付方式、日期选择、付款人多选、分摊四模式+预览、关联行程二级联动、备注）**全部迁入「更多选项」展开态**：点「更多选项」或摘要 chip → 页内 AnimatedCrossFade 展开（非弹层），一次只展开一组，自动滚到可视区。

### 8.4 算法规格（`lib/domain/money_expression.dart`，纯函数）

```dart
/// 连算表达式（仅 + − 与十进制金额，单位分）。
/// 输入键序如 [5,8,ADD,3,2] → 90 分；运算符连续/开头/结尾 → 忽略非法键；
/// 中间态显示当前表达式与实时合计；小数位 ≤2（分域截断）；上限 99,999,999 分。
class MoneyExpression {
  void pushDigit(int d); void pushOp(MoneyOp op); void pushDot();
  void backspace(); void clear();
  int? get totalFen;            // null = 当前无有效数值
  String get display;           // "58+32"
}
```

### 8.5 交互规格（逐条验收）

1. **完成=保存**：校验失败按序 SnackBar（请填名称/金额/付款人/让付款合计一致）——沿用现有校验文案；无激活团拦截防幽灵账单（沿用）；
2. **保存成功蒙层**：全屏玻璃蒙层 + 金额 CountUpText + heavyImpact，800ms 后自动 pop；期间「再记一笔」可点跳过等待；
3. **再记一笔**：金额清零、保留分类/支付方式/币种/日期（用户裁决字段集）；标题清空；连续计数徽章「今日第 N 笔」（第 3/5/10 笔触发 confetti_burst 复用）；
4. **默认值记忆**：SharedPreferences `app.exp.recent`（categoryKey/payMethod/currency），新建时回填；编辑模式不覆盖记忆；
5. **PopScope 草稿拦截**：有脏数据返回 → L2 确认弹层「放弃这笔账？」（正文列当前金额）；草稿存会话级内存 Map（key=`new` 或 `edit:{id}`），再次进入自动回填；保存/放弃后清除；**不持久化到磁盘**；
6. **个人账本模式**：隐藏付款人/分摊 chip 与区块（沿用现有判定）；viewer 到达编辑页的既有防线不动；
7. 键盘键帽 ≥48 逻辑像素高、`+−` 长按无效（防误触连发）、⌫ 长按 300ms 起以 80ms 连删。

### 8.6 测试用例清单（≥18）

连算 6（合法序/双运算符/开头运算符/小数进位/上限/清空）；键盘 2（完成保存、⌫连删）；折叠组 3（展开互斥/摘要 chip 同步/自动滚动）；草稿 3（拦截弹层/回填/清除）；记忆 2（回填/编辑不覆盖）；再记一笔 1（字段保留集断言）；蒙层 1（CountUp 后 pop）；校验 2（四类失败文案序/幽灵账单拦截）。合计 ≥18。

### 8.7 禁止

- 禁止金额浮点运算；禁止删除 `?id=`/`?date=` 路由参数契约（bill_detail/首页深链依赖）；禁止旧表单代码残留死分支（重写即清）；禁止改动 `/expenses/categories` 入口行为（仅从新页管理链接跳转）。

---

## 九、S6 明细页（A6）

### 9.1 涉及文件清单

| 动作 | 文件 |
|---|---|
| 新建 | `lib/shared/widgets/swipeable_bill_tile.dart` |
| 重写 | `lib/features/ledger/screens/expenses_screen.dart`（筛选区/分组/批量态） |
| 修改 | `lib/features/ledger/screens/ledger_home_screen.dart`（最近账单行换用 SwipeableBillTile） |
| 修改 | `lib/router.dart`（`/expenses` 支持 query 参数 `category/member/payMethods/from/to`——仅参数解析，零新增路由） |
| 新建 | `test/features/expenses_screen_test.dart` + `test/shared/swipeable_bill_tile_test.dart` |

### 9.2 规格要点（对照场景 D）

1. **SwipeableBillTile**：右滑露出编辑（mint 底）、左滑露出删除（red 底）；删除 → `showConfirmSheet(danger, quantityLine)` → repo 删 + 墓碑；左右滑动阈值 84 逻辑像素、回弹 240ms easeOutCubic、selectionClick 触发；**首页最近账单与本页为同一组件**（手势语言统一）；
2. **筛选单行化**：`全部 · 分类 pills 横滑 · 成员 pill · 📅时间 · ⚙更多`；支付方式多选移入「⚙更多」筛选抽屉（含清空）；「⚙」有激活筛选时显示数字徽章；
3. **双级吸顶**：月度头（`2026年10月 · 共 ¥9,214`）+ 日头（`10/02 周五 · 3 笔 · ¥356`），两级 `SliverPersistentHeader(pinned)`，月头玻璃 navBar 档；
4. **时间筛选**：📅 pill → `showDateRangePicker`；激活后 pill 显示区间并参与 URL 参数（供 S8 下钻）；
5. **批量**：长按任意行进入多选（勾选圈 + 顶部批量条：`已选 N 笔 · ¥X`）；动作 = 批量删除（L2 强确认含数 + 单事务 + 逐行墓碑）、批量改分类（分类选择抽屉 → 单事务逐行更新 + notifyWrite）；批量条玻璃 overlay 替代合计栏；单选反选/全选本天；viewer/只读不进入多选（隐藏）；
6. **合计栏**：GlassSurface overlay（当前筛选合计 + 笔数 + 「预付另计」沿用）；
7. 空态双分支沿用（筛选空 vs 从未记账）。

### 9.3 测试（≥14）：滑动暴露/回调/阈值回弹、批量删除事务回滚、批量改分类 notifyWrite、URL 参数五字段解析与组合、双级吸顶 pinned、时间筛选持久（仅会话内）、viewer 禁入口、合计口径含预付注记。

### 9.4 禁止：禁止引入第三方滑动库；禁止批量操作绕过事务；禁止改变 `/expenses` 无参时的既有默认行为。

---

## 十、S7 账本首页与功能入口收敛（A7）

### 10.1 涉及文件清单

| 动作 | 文件 |
|---|---|
| 重写（body 部分） | `lib/features/ledger/screens/ledger_home_screen.dart`（59KB 拆分：Hero 卡/快捷行/榜单/最近账单拆为 widgets/） |
| 新建 | `lib/features/ledger/widgets/ledger_hero_card.dart`、`ledger_quick_row.dart`、`ledger_toolbox_sheet.dart`（L3 工具箱）、`join_by_qr_tile.dart`（共享） |
| 修改 | `lib/features/ledger/screens/group_list_screen.dart`（团卡 Dismissible + JoinByQrTile） |
| 修改 | 切团抽屉（「全部」按钮 → 直跳 `/ledger/groups`） |

### 10.2 规格（对照场景 C + 用户裁决「去掉催TA」）

1. **三层节奏**：Hero 卡（合并团卡+预算卡+超支横幅+冲突 Chip 四层）→ 一行 4 玻璃圆钮（账单/统计/结算/快速记——**页面唯一记账入口是 FAB**，圆钮不出现第二个"记"）→ 成员榜+最近账单；公费池/收件箱/全部账单降级工具箱；
2. **Hero 卡**：团名+成员头像堆叠 / 76px 预算环（未配置→「添加预算」引导态沿用）/ 今日已花 CountUp / 三枚语义 chip（`未结 N 笔`→结算、`冲突 N`→审计、`预算 68%`→预算页）；超支态整卡 error 色调 + 顶部 4px 警示条；
3. **工具箱 = L3 操作抽屉**（零新增路由）：变更记录 / 局域网同步 / CSV 导入 / CSV 导出 / 分类管理，每行 icon+标题+副文案；头部溢出菜单唯一项改为「工具箱」入口；
4. **团管理去重**：切团抽屉底部「全部」→ `context.push('/ledger/groups')`；设置页入口保留为正门；团卡左滑删除（L2 强确认「将删除 X 与其全部账单/清单/想去池，不可恢复」）；
5. **JoinByQrTile**：替换 trips_home/group_list/share_center 三处扫码入口为同一组件（三处调用方本次允许触碰；trip_detail 禁触故不动其内部若有）；
6. FAB 玻璃圆钮化 + 长按快速记 sheet 沿用；
7. **无催 TA（O5）**；成员榜行保留垫付/应摊/应收结构。

### 10.3 测试（≥12）：Hero 三 chip 跳转、超支色调切换、工具箱五入口、切团抽屉直跳、删团 Dismissible+确认、QR 组件三处等价、空团/无预算引导态、viewer 隐藏写入口。

### 10.4 禁止：禁止新增顶层路由（工具箱走抽屉）；禁止动 `trips_home_screen` 主体（仅 QR 组件替换点）；禁止在快捷圆钮之外再放任何"记一笔"入口。

---

## 十一、S8 统计与预算（A8，含 schema N→N+1）

### 11.1 数据契约（SubBudgets，全量同步登记）

```dart
/// V2.8.1 分类子预算：团级，用户自选分类（上限 5）。
class SubBudgets extends Table {
  TextColumn get id => text()();
  TextColumn get groupId => text().references(Groups, #id)();
  TextColumn get categoryKey => text().withDefault(Constant("")); // 内置 key 或自定义 id
  IntColumn get amount => integer();                              // int 分
  IntColumn get createdAt => integer()();
  IntColumn get updatedAt => integer()();
  @override Set<Column> get primaryKey => {id};
}
```

- `schemaVersion N→N+1`（N = V2.7.2 交付后的实际值；若 V2.7.2 S1 未交付则顺延其目标值，迁移分支 `from < N+1`）；`docs/db_v281.sql` 新建（幂等，风格沿用 db_v271/272）；
- **同步登记 10 处**照抄 V2.7.2 §4.3(4) 范式（SyncEntity 枚举+localKey 显式映射 / codec / readBusinessRow / merger 五分支 / engine 闸门同口径 / requiredColumns / payload 测试 / 备份信封版本递增 / 级联：删团清子预算 / 领域镜像 SubBudgetRecord）；
- 若 V2.7.2 S1 未验收（schema 仍为 5），本阶段暂停 sync 登记仅建 drift 表 + 迁移，待其交付后补登记——**偏差登记**。

### 11.2 算法规格（`lib/domain/insight_engine.dart`，纯函数）

```dart
/// 洞察语（仅环比，O4：无集中日、无 AI）。
/// 输入本月/上月各分类总额（int 分）；输出至多 2 条：
/// 涨幅 = (cur-prev)/prev；触发条件：prev ≥ 5000 分 且 涨幅 ≥ 0.15（降）或 ≤ -0.15（升文案反义）；
/// 文案模板：「{分类}比上月多花 32%」（下降：「…省了 21%」）；数据不足（<2 月或当月 <10 笔）→ 空列表。
List<Insight> monthlyInsights({required Map<String,int> curMonth, required Map<String,int> prevMonth});
```

### 11.3 UI 规格

1. **下钻闭环**：分类圆盘扇区 / 成员横条 / 日柱 `touchCallback` → `context.push('/expenses?category=x&from=y&to=z')`（S6 参数筛选）；圆盘 top8 之外的「其他」不可点；
2. **洞察语**：图表区顶部玻璃胶囊（1–2 条，无则不渲染），点击跳该分类明细；
3. **时间范围**：S1 持久化 UI 接线（SegmentedButton 初始值从 prefs 读）；
4. **预算页**：总预算卡沿用；新增「分类子预算」区——添加（分类选择 sheet，已选置灰，**上限 5**，超出按钮禁用+提示）/ 每行进度条（80% amber / 100% red，暗色变体提亮 0.25）/ 编辑金额 / 删除（L2）；子预算金额**不计入**总预算口径（独立展示，总预算仍为单一总额）；
5. 月度趋势标签：端点强制显示+中间采样；柱状补 tooltip。

### 11.4 测试（≥16）：引擎 6（阈值边界/空数据/上升下降/多条排序上限 2）；迁移 2；同步 round-trip 2；上限 5 拦截 2；下钻路由参数拼接 2；双阈值色 1；删除子预算级联 1。

### 11.5 禁止：禁止子预算计入总预算；禁止洞察语任何网络/模型调用；禁止改 stats 既有图表数据口径（下钻仅消费现有聚合）。

---

## 十二、S9 账单详情抽屉（A9）

规格（场景 F）：结清 SwitchListTile → 头部状态胶囊（amber `待结清·点此标记两清` ↔ green `已两清✓`，点按 toggle + lightImpact）；分摊区 → 「谁付了·谁分摊」迷你卡（头像行 + 模式标签 chip + tabular 金额）；关联行程卡玻璃化 + 「解除关联」收进 ⋯ 菜单（L2 危险态）；底部 编辑（Filled 62% 宽）+ 删除图标钮（L2 含数量）；**精要插槽**：底部虚线占位段，V2.7.2 S10 `guide_essence_section` 完工后自动渲染（本版只留布局槽位+空态隐藏）；金额 MoneyHeroText 分级。测试 ≥8。禁止：动 `guide_essence_section`（V2.7.2 占用）；动结清业务语义（仅 UI 前置）。

---

## 十三、S10 设置页（A10）

规格（场景 J）：用户卡 → 状态摘要玻璃卡（emoji 头像保留=内容层；三 chip：上次同步时间（同步 provider）/ 当前团名 / 进行中行程数——跨域只读查询走既有 providers，不新增同步实体）；组内搜索（放大镜展开过滤框，tile 标题+副标题 contains 匹配）；预算预警 tile 整体 toggle（S1 已修，本阶段重排）；「账本工具箱」tile（跳账本页并自动开箱：路由带 `?toolbox=1` 或回调——**取零路由方案：S7 工具箱抽屉的触发点增加本页深链复用 `context.push('/ledger')` 后手动 show**，偏差登记选型）；分组重排（偏好/数据与隐私/其他）+ tile 图标（S4）；意见反馈 = 复制反馈邮箱 + toast（O 零依赖，真渠道登记后续）。测试 ≥8。禁止：动 cloud_account/sync_center 内部（S3 已迁弹层除外）。

---

## 十四、S11 微交互包 A（A11）

1. **PressableScale**（`lib/shared/widgets/pressable_scale.dart`）：按下 scale 0.97 / 90ms easeOut / lightImpact；替换账本域瓦片/圆钮/团卡 GestureDetector（不动 trips 域）；
2. **EmptyState 升级**：新增可选 `icon` 参数（AppIcons）与玻璃圆盘底座 + 3.2s 微浮动（±6px，`disableAnimations` 时静止）；emoji 参数保留兼容（既有调用零破坏）；账本域调用点逐一切换 icon；
3. **showAppSnackBar 全面接线**：账本域页面 SnackBar 统一走 S3 组件；
4. 保存成功蒙层、CountUp 复用已有（S5 已接，回归）。
测试 ≥6。禁止：动 trips/space 域调用点。

---

## 十五、S12 门禁与交付

### 15.1 三道门禁

| 门禁 | 标准 |
|---|---|
| 静态分析 | `flutter analyze` 0 error；warning ≤ 基线 |
| 测试 | 全绿；总数 ≥ `N1 + 120` |
| 构建 | `flutter build apk --release --target-platform android-arm64` 成功（Web 构建不做本版验收项，O7） |

### 15.2 全局回归清单（≥10）

迁移矩阵（schema N-1→N 升级数据零丢失）/ 同步 round-trip ×2（SubBudgets 本人+镜像）/ 三端冒烟（Android 手机/平板窄宽/旅伴空间账本区只读——空间 UI 未动仅回归不破坏）/ viewer 全禁用走查 / 深浅 12 主题 × 玻璃四 level 截图走查 / 账单全生命周期（记→改→结→删）/ 批量删除+改分类与云同步 / 下钻链路（统计→明细→编辑→返回）/ 草稿拦截与恢复 / 版本号 `2.8.1+2810`。

### 15.3 交付物

`docs/V2.8.1验收报告.md`（含 §偏差登记，格式沿用 V2.7.2 §16.4）、`docs/V2.8.1构建日志.txt`、`docs/db_v281.sql`、`docs/V2.8.1截图/`（深浅 × Hero卡/记账键盘/明细批量/统计下钻/抽屉/设置卡 ≥12 张）、release APK。

---

## 十六、风险与回退

| 风险 | 影响 | 缓解 | 回退 |
|---|---|---|---|
| V2.7.2 并行改到同一文件 | 高 | §1.4 禁触清单 + 每阶段 git status 对照 | 回滚该阶段 |
| GlassSurface 在低端机掉帧 | 中 | disableAnimations 降级 + overlay 档可静态化 | level 全局 fallbackOpaque 开关（DebugPanel 登记） |
| 键盘化丢失旧字段 | 高 | 8.3 第 6 条字段迁移清单逐项核对 + 旧页 diff 走查 | 旧实现 git tag 保留一键回退分支 |
| SubBudgets 同步缺陷 | 高 | 照抄 V2.7.2 S1 十处范式 + payload 测试 | 关闭添加入口（隐藏不置灰） |
| 批量操作误删 | 中 | L2 强确认含数量 + 单事务 | 无法恢复（O9 口径），靠确认拦截 |
| 图标字体漏字形 | 低 | codepoints 一致性测试 | fallback 显示 Icons 兜底图标 |

---

## 附录

### T1 场景 → 阶段映射（preview/index.html）

| 场景 | 阶段 |
|---|---|
| A 玻璃对比 | S2 |
| B 记一笔 | S5 |
| C 首页仪表盘 | S7 |
| D 明细页 | S6 |
| E 微交互 | S3（SnackBar）/ S11（其余） |

### T2 玻璃档位速查

navBar 阴影×0.6 / sheet ×1.0 / floatingCard ×1.2 tint×0.92 / overlay ×0.8 tint×0.95；σ=28、sat=1.35 全档通用。

### T3 迁移基线核对命令

`grep -rn "AlertDialog(" lib/features/ledger lib/features/settings --include=*.dart | wc -l`（S3 后 = 0）；`grep -rn "BackdropFilter(" lib --include=*.dart`（S2 后仅 glass_surface.dart）。

### T4 emoji 内容岗位白名单（S4 后账本域允许残留）

行程封面 chip（数据字段）、EmptyState emoji 兼容参数（未迁移调用）、用户输入内容、个人备注。其余位置出现 emoji 字符 = 违规。

### T7 用户裁决记录（三轮，效力等同条款）

R1：拆分方案 A 施行；H 进 2.8.2 仅页签层；M 仅登记 V2.8.3。R2：记一笔完全替换；批量=删除+改分类；洞察仅环比；子预算自选上限 5；维持纯 PIN 零新增依赖。R3：AlertDialog 全量分批迁移；**去掉催 TA**；H 仅页签层；HTML 移入 docs/V2.8/preview 并加阶段标注。

*本规格书为 V2.8.1 施工唯一依据。与既有文档冲突处以本文档为准；偏离走偏差登记流程。§〇 版权红线效力最高。*

